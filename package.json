{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build && npm run copy-build",
    "copy-build": "cp -r .output/public dist",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages --dotfiles -d .output/public"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "gh-pages": "^6.0.0",
    "nuxt": "^3.7.3"
  },
  "dependencies": {
    "swiper": "^8.4.7",
    "uuid": "^9.0.1"
  }
}
